import { describe, it, expect } from 'vitest';
import LoginForm from './LoginForm';

describe('LoginForm', () => {
  it('exports component or function', () => {
    expect(LoginForm).toBeDefined();
  });
});
