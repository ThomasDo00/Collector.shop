import { describe, it, expect } from 'vitest';
import Typography from './Typography';

describe('Typography variants', () => {
  it('exports and can be used', () => {
    expect(Typography).toBeDefined();
  });
});
